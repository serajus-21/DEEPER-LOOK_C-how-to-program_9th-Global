#define PI 3.1416
#define gEarth 9.8

#define unitArea sqMeter

#define sqr(x) ((x)*(x))
#define qb(x) ((x)*(x)*(x))
#define _areaOfCircle(const, radius) (const * radius * radius)
#define areaOfRectangle(width, length) (width * length)